{
  "name": "aletheiadocs",
  "version": "1.0.0",
  "private": true,
  "description": "AletheiaDocs - Filipino professional document center with tools, forum, and chat. Railway-ready.",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "build": "npm --prefix client run build",
    "start": "node server/index.js",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}